---
phase: 02-action-gate-residual-mappo
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_action_gate_mappo.py
autonomous: true
requirements:
  - RES-03
  - RES-04

must_haves:
  truths:
    - "joint_logp equals log P(gate) for gate=0 transitions"
    - "joint_logp equals log P(gate) + log P(phase) for gate=1 transitions"
    - "phase head receives zero gradient for gate=0 transitions during PPO update"
    - "gate=0 always executes the MP action, gate=1 always executes the phase head action"
  artifacts:
    - path: "tests/test_action_gate_mappo.py"
      provides: "Red tests that will fail until mappo.py implements action gate"
      min_lines: 60
  key_links:
    - from: "tests/test_action_gate_mappo.py"
      to: "ece324_tango/asce/mappo.py"
      via: "imports GatedActor, ResidualMAPPOTrainer"
      pattern: "from ece324_tango.asce.mappo import"
---

<objective>
Write failing unit tests that pin the joint log-probability semantics and gradient isolation requirement before any implementation.

Purpose: The joint logp formula is the highest-risk piece of the action-gate architecture. A wrong implementation passes silently — the PPO ratio is computed, gradients flow, and training appears to proceed normally even if the phase head receives gradient on gate=0 transitions. These tests catch that before a full training run reveals the bug. Tests must fail on the current codebase (no GatedActor exists yet) and pass after Plan 02 lands.

Output: tests/test_action_gate_mappo.py with five test functions covering joint logp value, gradient isolation, action selection dispatch (gate=0 → MP, gate=1 → phase), gate warm-start bias direction, and backward-compat (residual_mode="none" unchanged behavior).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md

@ece324_tango/asce/mappo.py
@tests/test_mappo_core.py
</context>

<feature>
  <name>Action-gate joint log-probability correctness</name>
  <files>tests/test_action_gate_mappo.py, ece324_tango/asce/mappo.py</files>

  <behavior>
    The classes to be imported (will not exist until Plan 02 is complete — tests must FAIL now):
      - `GatedActor(obs_dim, n_actions, hidden_dim, gate_init_bias)` — Actor with gate_head
      - `ResidualMAPPOTrainer(obs_dim, global_obs_dim, n_actions, residual_mode, ...)` — extended trainer

    Test 1 — joint_logp_gate0_equals_gate_logp:
      Setup: ResidualMAPPOTrainer(obs_dim=4, global_obs_dim=4, n_actions=3, residual_mode="action_gate")
      Force gate_head bias so gate=0 is deterministically sampled (e.g., gate_head.bias = [-100, 100] → high logit for gate=0 index)
      Call act_batch_residual(obs_list, global_obs, n_valid_actions_list, mp_actions_list=[1])
      Expected: returned logp == log P(gate=0); phase logp is NOT added

    Test 2 — joint_logp_gate1_equals_sum:
      Setup: same trainer but force gate=1 (gate_head.bias = [100, -100])
      Call act_batch_residual, capture phase logp separately from phase head
      Expected: returned logp == log P(gate=1) + log P(phase); within 1e-5 tolerance

    Test 3 — gate0_phase_head_zero_gradient:
      Setup: ResidualMAPPOTrainer, craft batch with all gate=0 transitions
      Capture phase_head parameter .grad before update
      Call trainer.update(batch) with ppo_epochs=1
      Expected: all phase_head weight gradients are zero (or None) after update on gate=0-only batch
      Method: assert all(p.grad is None or p.grad.abs().max() < 1e-9 for p in trainer.actor.phase_head.parameters())

    Test 4 — gate0_dispatches_mp_action:
      Setup: force gate=0; pass mp_actions_list=[2] for single agent
      Expected: final_action == 2 (the MP action), regardless of phase head output

    Test 5 — gate1_dispatches_phase_action:
      Setup: force gate=1; set phase head bias so action 0 has highest logit
      Expected: final_action == 0 (phase head choice)

    Test 6 — gate_warm_start_biases_toward_zero:
      Setup: GatedActor(obs_dim=4, n_actions=3, gate_init_bias=-3.0)
      Forward pass on random obs, sample 200 times
      Expected: fraction of gate=0 samples > 0.85 (bias toward follow-MP)

    Test 7 — residual_mode_none_unchanged:
      Setup: ResidualMAPPOTrainer(residual_mode="none")
      Call act_batch (original signature, no mp_actions_list)
      Expected: returns same structure as current MAPPOTrainer.act_batch — list of dicts with "action", "logp", "value"
      Expected: trainer.actor has no gate_head attribute (or gate_head is None)
  </behavior>

  <implementation>
    RED phase: Write all 7 test functions importing GatedActor and ResidualMAPPOTrainer.
    All tests should fail with ImportError or AttributeError since these classes don't exist yet.
    Commit with message: "test(02-01): add failing action-gate joint logp tests"

    GREEN phase: Happens in Plan 02-02 and 02-03 when the implementation lands.
    Do NOT implement GatedActor or ResidualMAPPOTrainer in this plan.
  </implementation>
</feature>

<verification>
  <automated>cd /home/as04/ece324-TANGO && pixi run python -m pytest tests/test_action_gate_mappo.py -x --tb=short 2>&1 | head -30</automated>
  After RED commit: all 7 tests should fail (ImportError or AttributeError — not unexpected errors like syntax issues).
  After GREEN (post Plan 02-02/02-03): all 7 tests pass.
</verification>

<success_criteria>
- tests/test_action_gate_mappo.py exists with 7 test functions
- Running pytest on it now produces 7 failures (ImportError or AttributeError), not syntax errors
- Test names follow project pattern: test_gate0_dispatches_mp_action, test_joint_logp_gate1_equals_sum, etc.
- Tests use only pytest, numpy, torch — no SUMO dependencies
- No production code is written in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-action-gate-residual-mappo/02-01-SUMMARY.md`
</output>
